{% extends "base.html" %}
{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h2>My Training</h2>
  {% if user.role == "admin" %}
    <a class="btn btn-sm btn-outline-secondary" href="{{ url_for('admin_dashboard') }}">
      Admin Dashboard
    </a>
  {% endif %}
</div>

{% if modules %}
  <div class="row">
    {% for m in modules %}
      {% set p = progress_map[m.id] %}
      <div class="col-md-4 mb-3">
        <div class="card h-100">
          <div class="card-body">
            <h5 class="card-title">{{ m.title }}</h5>
            <p class="card-text">{{ m.description }}</p>
            <p class="small text-muted">
              Progress:
              {% if p.total == 0 %}
                No lessons yet
              {% else %}
                {{ p.completed }}/{{ p.total }} lessons
              {% endif %}
            </p>
            <a href="{{ url_for('module_detail', module_id=m.id) }}" class="btn btn-primary btn-sm">
              Open Module
            </a>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
{% else %}
  <p>No active training modules yet.</p>
{% endif %}
{% endblock %}
